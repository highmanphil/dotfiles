#!/usr/bin/env bash
set -euo pipefail

# kubectl passes this executable everything after `kubectl secret`, including
# the unfinished final word. Translate that command line to the native Secret
# getter and return Cobra's completion directive unchanged.
exec kubectl __complete get secret "$@"
